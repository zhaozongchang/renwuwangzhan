<div class="col-md-12">
  <div class="task">
    <%= link_to("Edit", edit_task_path(@task), class: "btn btn-primary pull-right")%>
  </div>
  <h2><%= @task.title %></h2>
  <p><%= @task.description %></p>
</div>
